{
  "name": "@neo-quiz/windows",
  "version": "1.12.0",
  "private": true,
  "type": "module",
  "main": "dist-electron/main.cjs",
  "scripts": {
    "postinstall": "patch-package",
    "dev": "concurrently -k -n vite,electron \"vite\" \"npm run dev:electron\"",
    "dev:electron": "npm run build:electron && electron dist-electron/main.cjs",
    "typecheck": "tsc --noEmit",
    "typecheck:electron": "tsc --noEmit -p tsconfig.electron.json",
    "typecheck:installer": "tsc --noEmit -p installer/tsconfig.node.json && tsc --noEmit -p installer/tsconfig.renderer.json",
    "build:electron": "node electron/construire.mjs",
    "build:installer": "npm run typecheck:installer && node installer/construire.mjs",
    "build": "npm run typecheck && npm run typecheck:electron && npm run typecheck:installer && npm run build:electron && vite build",
    "pack:win": "npm run build && electron-builder --win nsis --publish never --config electron-builder.config.mjs",
    "pack:installer": "npm run build:installer && electron-builder --win portable --publish never --config installer/electron-builder.config.mjs",
    "pack:linux": "npm run build && electron-builder --linux --publish never --config electron-builder.config.mjs"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "electron-updater": "^6.8.9",
    "lucide": "^1.41.0",
    "pdfjs-dist": "^5.7.284"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "electron": "^44.3.0",
    "electron-builder": "^26.0.19",
    "esbuild": "0.28.2",
    "patch-package": "^8.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  }
}
